import { PipeIdBookPipe } from './pipe-id-book.pipe';

describe('PipeIdBookPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeIdBookPipe();
    expect(pipe).toBeTruthy();
  });
});
